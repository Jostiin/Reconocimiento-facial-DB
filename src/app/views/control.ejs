<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel de Control</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="css/control.css">
</head>
<body>
    
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark ">
      
    <a class="navbar-brand" href="/">
      <img src="https://wide.ec/wp-content/uploads/2022/06/logo-widep.png" style="width:100px;" class="rounded-pill"> 
    </a>
    
  
  </nav>
    <h2>Panel de control</h2>
    <div class="container col-md-6 container-md ">      
        
        <div id="dsTable" class="container container-table" >           
          <table class="table table-hover table-bordered  table-dark table-responsive">
            <thead>
              <tr>
                <th class="info">ID</th>
                <th class="info">Nombre</th>
                <th class="info">Entrada</th>
                <th class="info">Salida</th>
                
              </tr>
            </thead>
            <tbody>
                <% for(var i=0; i<users.length; i++) {%>
                  <tr>
                        <td><%= users[i]["id"] %></td>
                        <td><%= users[i]["Nombre"] %></td>
                        <td><%= users[i]["Entrada"] %></td>
                        <td><%= users[i]["Salida"] %></td>
                  </tr>
                <% } %>

            </tbody>
          </table>
        </div>
        </div>
</body>
</html>